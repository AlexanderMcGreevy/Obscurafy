# Copy this file to .env for local development (DO NOT COMMIT .env)
# Fill values locally; keep real secrets out of source control.

# Backend server port
BACKEND_PORT=3000

# Gemini (or other generative) API key — REMOVE/KEEP PRIVATE (do not commit)
GEMINI_API_KEY=REPLACE_WITH_REAL_KEY

# Gemini endpoint (adjust to the real API endpoint you use)
GEMINI_ENDPOINT=https://GENERATIVE_API_ENDPOINT

# Optional
NODE_ENV=development